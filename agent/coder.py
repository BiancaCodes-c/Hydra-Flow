"""Coder: generates transforms/checks code from natural language (placeholder).

These are safe stubs — real implementation should sandbox code generation and review.
"""

def generate_transform(spec: str) -> str:
    """Return Python function source for a transform (string)."""
    return (
        "def transform(df):\n"
        "    # TODO: implement transform according to spec\n"
        "    return df\n"
    )
